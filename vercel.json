{
  "version": 2,
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next"
    },
    {
      "src": ".storybook",
      "use": "@vercel/static-build",
      "config": {
        "outputDirectory": "storybook-build"
      }
    }
  ],
  "routes": [
    {
      "src": "/",
      "dest": "/index.html",
      "headers": {
        "x-branch": "${VERCEL_GIT_COMMIT_REF}"
      }
    },
    {
      "src": "/storybook/(.*)",
      "dest": "/storybook-build/$1",
      "headers": {
        "x-branch": "${VERCEL_GIT_COMMIT_REF}"
      }
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "github": {
    "mainBranch": "main",
    "autoAlias": true
  }
}
